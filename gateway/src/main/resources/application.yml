spring:
  application:
    name: gateway
  config:
    # Everything else, routes included, comes from the config server.
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}
  cloud:
    config:
      fail-fast: ${CONFIG_FAIL_FAST:false}
